Assembles the hierarchical node graph that every analysis module operates on — wiring parent-child pointers, detecting helpers, propagating util flags, and materializing pattern groups. It is the only module that constructs the shared structural backbone — nothing else transforms parsed units into the navigable map of directories, files, functions, and types.
