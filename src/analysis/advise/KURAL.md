Evaluates whether a directory's children would benefit from regrouping — clustering them by affinity, proposing cuts at natural fault lines, and simulating what the resulting metrics would look like. It is the only module that computes hypothetical reorganization outcomes — nothing else answers "what if I split this?"
