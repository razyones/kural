Converts raw source files into numerical representations — reading TypeScript from disk, distilling each unit into descriptive prose, and crossing the network to produce embedding vectors. It does not assemble the navigable graph or compute scores; it only prepares the per-unit inputs that downstream stages consume.
