The compass. Routes a natural-language query through conditional probability chains, local projections, and alien/bridge detection tiers to determine where new code belongs in the directory tree. It is the only part of the system that answers "where should this live?" — nothing else navigates the tree structure, classifies placement candidates, or decides between auto-placing and deferring to the user.
