The blueprint. Houses sensitivity dials for diagnostics, reference anchors for measurement axes, provider connection shapes, and the disk reader that loads them at startup. It is the only module that owns declarative tuning and reference data — no other module defines detection thresholds, axis sentences, provider shapes, or the file-loading contract.
