The blueprint. Houses every knob, shape, and constant the system reads before it runs. It is the only module that owns declarative settings — no other module defines tuning parameters, reference data, or configuration contracts.
@kuralResidual containments [bb34f432]
