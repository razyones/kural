# Changelog

## v0.0.1-alpha.1 — 2026-04-05

### Features

- add automated changelog generation and release script (`6c0e667`)
- add @kuralBorrows to shell commands and persist in snapshot (`5eb58b6`)
- add @kuralBorrows directive for cross-layer vocabulary borrowing (`95e089a`)
- add place command with four-tier routing engine (`0895974`)
- add advise command for directory restructuring analysis (`28fa9a8`)
- add weak-identity audit to detect drifting directory children (`f71c82d`)
- add owl mascot to README and CLI banner (`0d4d320`)
- hybrid search with pre-built index and semantic embeddings (`bd4e38d`)
- advanced search with headings and content snippets (`2980865`)
- add getting-started guide, sidebar icons, and docs restructure (`54b2734`)
- run embedding extraction before docs dev and build (`0b3117f`)
- animated background, embedding space refinements, and docs styling (`36dc787`)
- add interactive embedding space visualization to homepage (`4a30f4a`)
- add Cards, Tabs, Steps, and Callouts to docs (`e03b370`)
- add full-text search via server function (`6d3cda7`)
- add sidebar ordering, TOC sidebar, and docs-site improvements (`859830e`)
- add docs:dev and docs:build convenience scripts (`6c3f127`)
- add Fumadocs documentation site with TanStack Start (`b7b2394`)
- add snapshot pinning and reorganize CLI under snapshot namespace (`549921f`)
- tag cli.ts as inward-bound and renderHero as outward-bound (`2c0c486`)
- add @kuralBound inward/outward param for hierarchically bound units (`8ca362c`)
- support comma-separated @kuralPatterns syntax (`6ffb49e`)
- support nested pattern groups via multiple @kuralPatterns tags (`9eab837`)
- materialize pattern groups as in-memory tree nodes (`f7fee0b`)
- add --json flag to audit command (`53ca48f`)
- add glossary and next-step hints footer to audit command (`474d997`)
- load embedding cache from previous snapshot before re-embedding (`00c6aad`)
- display snapshot ID in generate output (`d88fa20`)
- add pluggable audit system with 12 structural audits (`1162861`)
- add score command with hero display, breakdown table, and footers (`678425b`)
- add --json flag to generate command (`756f487`)
- add cliui banner and spinner progress to generate command (`53465b3`)
- three-perspective scoring and branch-scoped snapshots (`af6c1fb`)
- wire generate command as root CLI entry point (`8bfe9e8`)
- implement V2 scoring with CV-based uniqueness (`0ded906`)
- add db module with TanStack DB and SQLite persistence (`7137309`)
- port ingestion pipeline with 90% coverage threshold (`2ec20b5`)
- scaffold kural CLI project (`d33089c`)

### Fixes

- enforce coverage in pre-commit hook and expand test coverage (`c41e948`)
- resolve vocabulary bleed and misplaced audit findings (`07c77fd`)
- render sidebar icons in docs site and add owl to introduction (`c53482a`)
- remove docs:build script and inline steps in CI (`dc2d791`)
- show breadcrumbs on all search results and restore hover highlight (`bb9ce15`)
- resolve h3 vulnerability by pinning to 2.0.1-rc.20 (`91fba2a`)
- prevent stale search results persisting across query changes (`a88d7d0`)
- prevent layout shift on search dialog open/close (`c26f68a`)
- use module-level flag instead of useRef in StableSearchItem (`4739267`)
- prevent search list jumping on pointer hover (`0b7a36e`)
- clean up search results — breadcrumbs only on pages, filter noise (`dfd6729`)
- return fumadocs SortedResult format from search with highlights (`79fbd09`)
- client-side search — build Orama index at runtime from embedded data (`5793cb6`)
- use fumadocs' initSimpleSearch for compatible search index format (`f607b84`)
- use correct search client type — "static" not "orama-static" (`176bd8a`)
- match fumadocs search schema — add breadcrumbs and keywords fields (`c5b2720`)
- replace server-side search with static Orama index (`08e78d7`)
- add root-level 404.html for GitHub Pages SPA routing (`0688e3e`)
- use fully static docs route — no server functions needed (`7160f0a`)
- embed page tree in client bundle for static hosting fallback (`83147f1`)
- make docs route work on static hosting without server functions (`31e41e7`)
- restore BASE_PATH and stabilize e2e test timeouts (`057814f`)
- add SPA fallback pages for GitHub Pages static hosting (`399cd1f`)
- restore SPA prerendering with nitro@3 workaround (`010fc9e`)
- disable prerender — blocked by nitro@3 + TanStack Start race condition (`d3c7fb6`)
- resolve docs build and formatter issues (`0b08316`)
- remove BASE_PATH from docs build — prerenderer requires root base (`1dc4311`)
- make extract-embeddings portable and add snapshot generate to docs CI (`c9895b0`)
- skip embedding extraction when database is missing (`9476775`)
- stabilize e2e snapshots and add docs links to README (`5ac22b6`)
- resolve CI failures and stabilize e2e snapshots (`502558e`)
- resolve all lint and type check failures (`7f53d11`)
- resolve 8 dependabot security vulnerabilities (`055d73b`)
- add error handling across filesystem, database, and API boundaries (`25e00d6`)
- correct documentation discrepancies and docs-site footer spacing (`fa40ae3`)
- strip leaked git env vars in e2e createTmpRoot (`2504d76`)
- preserve hyphens in --disable flag and add e2e test suite (`4d515da`)
- require 3+ members per cluster in bloated audit (`a6225c4`)
- tune MIN_MAD floor to 0.015 and set project sensitivity to 2.2 (`b88cb0d`)
- add MIN_MAD floor to robust fences and wire project config into generate (`0755ceb`)
- exclude helpers and utils from containment dominance check (`1f35c38`)
- require substantial clusters in bloated audit to eliminate false positives (`5f2c910`)
- unify mean/centroid and remove OutwardRef duplicate (`1e17f57`)
- update residual hash for config/ containment suppression (`52a0720`)
- extend isCallerCallee to detect function-to-pattern relationships (`c650cd8`)
- preserve snapshot ID when rotating active database to history (`754e5e5`)
- nest fence and signature patterns to resolve merge-candidate findings (`17d1e8e`)
- use # prefix for audit filter IDs in report output (`8fb0f63`)
- add writeDirectories to writeCollection pattern group (`5232a8f`)
- normalize hyphens in audit filter and show filter IDs (`d9d652b`)
- update expired residual hash for src bloated-directories (`509eda0`)
- narrow audit pattern exclusions to same-group pairs (`0c0ebe2`)
- score util containers within their own tree (`cdbe9e9`)
- compute leaf embedding for pattern nodes (`0754dc4`)
- add residual, helper, and pattern tags to resolve audit findings (`5cdf4d3`)
- add missing @kuralPatterns tags and fix vocabulary bleed in descriptions (`b2f460f`)

### Documentation

- add configuration page with sensitivity narrative and per-audit k map (`156a5a3`)
- add KURAL.md identity descriptions for existing directories (`9041d8f`)
- remove paid tier references from architecture doc (`4ec59da`)
- move lint rules into CLAUDE.md and complete README JSDoc examples (`eece2ef`)
- move database page under infrastructure folder (`4770538`)
- add README with installation, usage, and command reference (`e7c184f`)
- restructure documentation around the four pillars (`f28448e`)
- broaden hero.ts description to cover shared score presentation utilities (`574a5f6`)
- add missing @param for commitHash in writeMetadata (`583df32`)
- add missing JSDoc tags to sharesPattern in duplicates audit (`1effec5`)
- update pattern-nodes.md to reflect implemented behavior (`9783bb6`)
- differentiate writeUnits and buildProse descriptions from their pattern siblings (`9b90b55`)
- add JSDoc for isSameFileViaPatterns (`e97cd48`)
- add JSDoc for meanSiblingSimPerChild, pushLeaf, collectFileLeaves, collectLeaves (`cbdb018`)
- align outlier file descriptions with parent module vocabulary (`827b9a8`)
- differentiate detectHelpers and propagateUtil descriptions (`8295c76`)
- fix vocabulary bleed in commands/audit and embed/collect descriptions (`3a389dc`)
- add missing JSDoc to buildBanner in audit command (`cb265f3`)
- fix all incomplete-docs audit findings in generate command (`1371971`)
- fix all incomplete-docs audit findings across the codebase (`5fb09d9`)
- redesign scoring with three perspectives per node (`65f2657`)
- add scoring and embedding specs, make labelFit nullable (`8233be0`)
- add database architecture specification (`f1a82e8`)

### Refactoring

- restructure src/ into analysis/ and shell/ domains (`091478f`)
- merge util-duplicates into duplicates.ts (`5c8e706`)
- move collapseByPattern and blendLeaves to containers.ts (`11a73a5`)
- inline printListItem into printListSections to resolve bloated-file finding (`a4e7b62`)
- rename duplicate-utils to util-duplicates (`63553d8`)
- extract persistRows helper, add pattern tags, increase delta precision (`2185e26`)

### Tests

- add edge case coverage for @kuralBorrows parsing (`76c5db2`)
- add visual regression snapshot tests for CLI output (`f15a930`)
- complete e2e audit triggers for all 14 audits (`190bbab`)
- add misplaced and vocabulary-bleed e2e audit triggers (`6d5e560`)
- add e2e audit trigger tests for 9 of 14 audits (`2961045`)
- add tests for snapshot pinning, resolution, and eviction (`808a587`)

### CI

- add branch-based versioned docs deployment (`48f05ed`)
- add explicit permissions to CI workflow (`d48d90e`)
- add CI/CD workflows and gitignore .agents and .claude (`ddedbf2`)

### Chores

- add docs-site .gitignore and apply formatting fixes (`5ee3aa8`)
- gitignore internal architecture doc (`f09d79c`)
- install use-gunshi-cli skill via skills.sh (`3ae1b46`)
- add runtime dependencies and update build config (`8bedebc`)
- enable strict type checking and code organization lint rules (`2f5e8fc`)
